import StoryblokClient from 'storyblok-js-client'

const client = new StoryblokClient({
    accessToken: 'RpqP8CUj6nosXrCMCF757gtt'
})

export const defaultRequestConfig = {
    version: 'draft'
}

export default client; 